export interface UrlMapping {
    hash_id: string,
    actual_url: string,
    expiration_date: Date,
    creating_date: Date
}

export interface CreateUrlMapping{
    hash_id: string,
    actual_url: string,
    expiration_date: Date,
    creating_date: Date
}